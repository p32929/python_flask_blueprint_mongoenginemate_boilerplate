from enum import Enum
from pydantic import BaseModel
from typing import Optional

class Create${uname}Body(BaseModel):
    email: str
    password: str
    
class Update${uname}Body(BaseModel):
    email: str
    password: str
    